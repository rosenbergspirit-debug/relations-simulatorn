[project]
name = "relations-simulator"
version = "0.1.0"
description = "Ett turbaserat relationsspel för att förstå relationsdynamik"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
